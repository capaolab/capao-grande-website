services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: capao
      POSTGRES_PASSWORD: capao
      POSTGRES_DB: capao_grande
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
